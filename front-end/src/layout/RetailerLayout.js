import React from 'react'

const RetailerLayout = ({children}) => {
    return (
        <div>
            retailer layout
            {children}
        </div>
    )
}

export default RetailerLayout
